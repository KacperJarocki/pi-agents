apiVersion: v1
kind: Service
metadata:
  name: dashboard
  namespace: iot-security
  labels:
    app: dashboard
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 8080
      protocol: TCP
      name: http
  selector:
    app: dashboard
