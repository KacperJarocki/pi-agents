apiVersion: v1
kind: Service
metadata:
  name: gateway-api
  namespace: iot-security
  labels:
    app: gateway-api
spec:
  type: ClusterIP
  ports:
    - port: 8080
      targetPort: 8080
      protocol: TCP
      name: http
  selector:
    app: gateway-api
