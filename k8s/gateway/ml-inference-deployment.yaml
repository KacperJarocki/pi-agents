apiVersion: apps/v1
kind: Deployment
metadata:
  name: ml-inference
  namespace: iot-security
  labels:
    app: ml-pipeline
    component: inference
spec:
  replicas: 1
  selector:
    matchLabels:
      app: ml-pipeline
      component: inference
  template:
    metadata:
      labels:
        app: ml-pipeline
        component: inference
    spec:
      nodeSelector:
        node-role.kubernetes.io/gateway: "true"
      containers:
        - name: ml-inference
          image: ghcr.io/kacperjarocki/ml-pipeline:latest
          imagePullPolicy: Always
          command:
            - python
            - -m
            - app.inference
          resources:
            requests:
              cpu: 100m
              memory: 256Mi
            limits:
              cpu: 300m
              memory: 512Mi
          env:
            - name: DATABASE_PATH
              value: "/data/iot-security.db"
            - name: MODEL_PATH
              value: "/data/models"
            - name: LOG_LEVEL
              value: "info"
            - name: INFERENCE_INTERVAL
              value: "60"
            - name: INFERENCE_HOURS
              value: "24"
            - name: BEHAVIOR_BASELINE_HOURS
              value: "168"
            - name: FEATURE_BUCKET_MINUTES
              value: "2"
            - name: PER_DEVICE_MODELS
              value: "true"
          volumeMounts:
            - name: sqlite-data
              mountPath: /data
          livenessProbe:
            exec:
              command:
                - sh
                - -c
                - |
                  test -f /tmp/inference-heartbeat && \
                  test $(( $(date +%s) - $(cat /tmp/inference-heartbeat) )) -lt 900
            initialDelaySeconds: 120
            periodSeconds: 60
            failureThreshold: 3
          readinessProbe:
            exec:
              command:
                - sh
                - -c
                - "test -f /tmp/inference-heartbeat"
            initialDelaySeconds: 10
            periodSeconds: 30
      volumes:
        - name: sqlite-data
          persistentVolumeClaim:
            claimName: iot-security-sqlite
      securityContext:
        runAsNonRoot: true
        runAsUser: 1000
        fsGroup: 1000
