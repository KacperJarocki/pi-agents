apiVersion: apps/v1
kind: Deployment
metadata:
  name: gateway-agent
  namespace: iot-security
spec:
  template:
    spec:
      containers:
        - name: gateway-agent
          env:
            - name: ENABLE_APPLY
              value: "true"
            - name: AUTO_RESTORE
              value: "true"
